@model List<Brand>
@{ Layout = "_LayoutAdmin"; }

<div class="container-fluid">
    <h1 class="dash-title">@localizer["Markalar"]</h1>
    <div class="row">
        <div class="col-lg-12">
            <p>
                <a class="btn btn-primary" data-toggle="collapse" href="#collapseExample" role="button" aria-expanded="false" aria-controls="collapseExample">
                    @localizer["Marka Ekle"] 
                </a>
            </p>
            <div class="collapse" id="collapseExample">
                <div class="card card-body">
                    <form asp-controller="Admin" asp-action="BrandCreate" method="post">
                        <div asp-validation-summary="All" class="text-danger"></div>
                        <div class="form-group row">
                            <div class="col-sm-12 pb-3">
                                <label>@localizer["Marka Adı"]</label>
                                <input class="form-control" name="Name" />
                            </div>
                        </div>
                        <button type="submit" class="btn btn-primary">@localizer["Ekle"]</button>
                    </form>
                </div>
            </div>
            <div class="card easion-card">
                <div class="card-header">
                    <div class="easion-card-icon">
                        <i class="fas fa-table"></i>
                    </div>
                    <div class="easion-card-title">@localizer["Marka Bilgileri"]</div>
                </div>
                <div class="card-body ">
                    <table class="table table-hover table-in-card">
                        <thead>
                            <tr>
                                <th scope="col">@localizer["Marka Id"]</th>
                                <th scope="col">@localizer["Marka Adı"]</th>
                                <th scope="col">@localizer["Düzenle/Sil"]</th>
                            </tr>
                        </thead>
                        <tbody>
                            @foreach (var item in Model)
                            {
                            <tr>
                                <td scope="row">@item.Id</td>
                                <td>@item.Name</td>
                                <td>
                                    <a href="/admin/BrandEdit/@item.Id" class="btn btn-primary btn-sm mr-2"><i class="fas fa-edit"></i></a>
                                    <form action="/admin/BrandDelete" method="post" style="display:inline">
                                        <input type="hidden" name="BrandId" value="@item.Id" />
                                        <button type="submit" class="btn btn-danger btn-sm "><i class="fas fa-trash-alt"></i></button>
                                    </form>
                                </td>
                            </tr>
                            }
                        </tbody>
                    </table>
                </div>
            </div>
        </div>
    </div>
</div>